# Written by spool. Reproduced from spool.toml and spool.lock, so it is not
# source and does not belong in a commit.
twill_modules/
.spool/

# Model archives and scoring output a program reads or writes.
models/
*.slv
scores/

# The request fixtures examples/serve.tw generates on first use. Generated
# rather than checked in, because the source gate refuses a file in this
# repository that is not twill or documentation.
examples/*.csv
